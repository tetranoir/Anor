/**
 * Type for the result of ngraph.graph()
 */
export type NGraphGraph = any; // todo look for ngraph type, if there is one

/**
 * Type for the types of centrality in ngraph.centrality
 */
export type CentralityMode = 'eccentricity'|'degree'|'closeness'|'betweenness';
